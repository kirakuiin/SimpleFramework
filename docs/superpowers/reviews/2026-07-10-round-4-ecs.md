# Round 4: ECS

## Files Reviewed

- Production: `ECS/Archetype.cs`, `ECS/CommandBuffer.cs`, `ECS/Component.cs`, `ECS/ComponentColumn.cs`, `ECS/ECSExtension.cs`, `ECS/Entity.cs`, `ECS/EntityPrefab.cs`, `ECS/Query.cs`, `ECS/System.cs`, `ECS/SystemDependencyAttributes.cs`, `ECS/SystemGroup.cs`, `ECS/TypeSignature.cs`, `ECS/World.cs`, and `ECS/ECS.csproj`.
- Tests: every tracked file under `Test/ECS`: `UnitTestArchetype.cs`, `UnitTestCommandBuffer.cs`, `UnitTestComponent.cs`, `UnitTestEach.cs`, `UnitTestEcsSystem.cs`, `UnitTestEntity.cs`, `UnitTestEntityPrefab.cs`, `UnitTestQuery.cs`, `UnitTestSignature.cs`, `UnitTestSystemGroup.cs`, and `UnitTestWorld.cs`.
- User documentation: `ECS/README.md` and `ECS/USAGE.md`.
- Contracts: `AGENTS.md`, `docs/superpowers/specs/2026-07-10-full-codebase-review-design.md`, `docs/superpowers/plans/2026-07-10-full-codebase-review.md`, and the five earlier ECS design/implementation records under `docs/superpowers/specs` and `docs/superpowers/plans`.

## Contracts Checked

- Traced empty and component-backed creation, global monotonic IDs across worlds, destroy/swap-back bookkeeping, stale/forged/foreign handles, world reset, and live-entity enumeration.
- Traced add/update/set/remove migrations through source and target archetypes, boxed row transfer, component-column alignment, structural-version changes, query membership, and writable component references.
- Traced query include/exclude/clear, existing and newly-created archetypes, lazy refresh, destruction/migration observations, structural mutation rejection, cache exposure, and the current no-subscription/no-finalizer lifetime model.
- Traced command order, real and buffered targets, same-playback create/destroy, invalid target policy, failed playback, successful consumption, cross-buffer ownership, and result resolution.
- Traced prefab duplicate/null handling, struct-copy and class-reference semantics, instantiation, and example accuracy.
- Traced signature normalization, equality/hash consistency, component validation, and same-full-name types from distinct assemblies.
- Traced manual order, stable insertion order, dependency attributes, base-type matching, missing and cross-phase dependencies, cycles and diagnostics, cache invalidation, disabled systems, callback exceptions, group mutation, and reentrant update.
- Inspected obvious allocations and repeated work. Query refresh and system dependency sorting are cached. Structural migration still boxes/copies component maps by the explicitly retained lightweight-ECS design; changing that path would add complexity without measurement and was not justified in this round.

## Findings and Decisions

- Critical: `SystemGroup.Update` allowed reentrant dispatch. A regression test reproduced an actual stack overflow and test-host crash. The group now rejects nested update before sorting or dispatch and recovers through the existing `finally` guard.
- Important: `Archetype.Add` appended the entity and earlier columns before discovering a missing later component, corrupting row/column alignment. It now validates and collects the complete row before committing storage changes.
- Important: `BufferedEntity` identity used only a buffer-local integer, so equal local IDs from different buffers aliased during command recording and result resolution despite the approved ownership contract. Identity now includes a unique command-buffer owner, and foreign buffered targets are rejected before recording.
- Important: `TypeSignature` ordered types only by `FullName`; distinct component types with the same full name in different assemblies compared as the same binary-search key. Assembly-qualified identity now breaks the tie while preserving deterministic order-independent equality/hash behavior.
- Important: `Query.GetArchetypes` returned its mutable internal `List<Archetype>` as `IReadOnlyList`, allowing a cast to clear query state. It now returns one cached read-only wrapper, avoiding both mutation and per-access copies.
- Moderate: null class components were inconsistently dereferenced, recorded, or stored. `World`, `CommandBuffer`, and `EntityPrefab` now reject them at public component-taking boundaries before state changes, with explicit parameter diagnostics.
- Moderate: `EcsSystem` accepted a null world into its protected non-null contract. Construction now fails immediately with `ArgumentNullException` naming `world`.
- Documentation: the supplied baseline filter named a nonexistent namespace and selected zero tests. The plan now uses `FullyQualifiedName~Test.ECS`, which selected all 97 baseline ECS tests and all 107 final ECS tests.
- Documentation: usage text incorrectly limited components to value types and allocated a new query inside every example frame. It now documents struct/class/null semantics, caches system queries, and includes accurate command-buffer, prefab, ownership, ordering, and non-transactional playback examples.
- Retained by design: entity IDs remain static and monotonically increasing across worlds; invalid `DestroyEntity` remains a no-op returning false; failed command playback stops after earlier effects and retains commands; prefab class components remain shared references; queries use lazy archetype-version refresh without subscriptions or disposal; missing dependencies and cross-manual-order edges remain ignored; disabled systems remain in dependency validation; direct structural mutation during query traversal remains rejected.

## Changes

- Added ten deterministic regression tests covering transactional archetype insertion, cross-buffer placeholders/results, null component boundaries, immutable query exposure, colliding type names, null system worlds, and reentrant scheduling recovery.
- Hardened `Archetype`, `CommandBuffer`, `EntityPrefab`, `Query`, `EcsSystem`, `SystemGroup`, `TypeSignature`, and `World` with the minimum behavior needed by those tests.
- Updated every changed public contract with Chinese XML documentation and corrected the ECS README/usage examples.
- Appended seven exact RED/GREEN repair subtasks to the full-review plan before each production change.

## Verification

- Required baseline command with `FullyQualifiedName~SimpleFramework.Test.ECS`: zero tests selected, exposing the stale filter.
- Corrected baseline `dotnet test .\Test\Test.csproj --no-restore --filter "FullyQualifiedName~Test.ECS"`: 97 passed, 0 failed, 0 skipped before repairs.
- RED: ten new tests failed for their intended defects; the reentrant scheduling test additionally proved the process-crash failure mode.
- Focused final ECS command: 107 passed, 0 failed, 0 skipped.
- Full `Test` project: 730 passed, 0 failed, 0 skipped.
- Release solution build: succeeded with 0 warnings and 0 errors.
- Source-text quality fixture: 1 passed, 0 failed.
- `git diff --check`: no whitespace errors. Corrupted-Chinese scan returned no matches.

## Independent Review

No independent reviewer was dispatched because the task brief explicitly assigned implementation only and instructed this worker not to dispatch a reviewer. The implementation range starts at `29f30e39fb29ec7c13458e1e61652066750f4c44`; independent review and its disposition remain the parent workflow's responsibility after the round commit.
